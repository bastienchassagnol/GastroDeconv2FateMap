# Pinned stack for GRNITE-style workflows (CUDA 12.1 PyTorch wheels).
# torch / torchvision / torchaudio are installed first in install_grnite_uv.sh
# via the cu121 extra index (see script).
#
# tables: needed for pandas.read_hdf on Gene_emebddings/*.h5 in Main.py.

numpy==2.2.6
pandas==2.3.1
scikit-learn==1.6.1
scipy==1.15.3
torch-geometric==2.6.1
tqdm==4.67.1
seaborn==0.13.2
matplotlib==3.10.0
tables==3.10.2
