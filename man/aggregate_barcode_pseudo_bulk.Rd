% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_pseudo_bulk_samples.R
\name{aggregate_barcode_pseudo_bulk}
\alias{aggregate_barcode_pseudo_bulk}
\title{Aggregate pseudo-bulk samples by barcode (organoid)}
\usage{
aggregate_barcode_pseudo_bulk(
  seurat_obj,
  phenotype_col = "Morphotype",
  barcode_col = "Sample.barcode",
  assay = "RNA",
  cell_mask = NULL
)
}
\arguments{
\item{seurat_obj}{A \pkg{Seurat} object with raw counts in the RNA assay.}

\item{phenotype_col}{Metadata column for organoid-level phenotype labels
(e.g. \code{"Morphotype"}).}

\item{barcode_col}{Metadata column for barcode / organoid identifiers
(e.g. \code{"Sample.barcode"}).}

\item{assay}{Assay name from which raw counts are read. Default
\code{"RNA"}.}

\item{cell_mask}{Optional logical vector aligned with \code{seurat_obj}
cells; when provided, only these cells are aggregated.}
}
\value{
A \pkg{SummarizedExperiment} with one column per distinct barcode.
\code{colData} contains \code{barcode_id}, the phenotype label,
\code{library_depth} (column sum of counts), and
\code{simulation_method = "barcode_aggregation"}.
}
\description{
Strategy 1 from \code{vignettes/pseudo_bulk_generation.qmd}: treat each
barcode as one bulk sample by summing raw single-cell counts within
barcodes, analogous to \code{\link[Seurat]{AggregateExpression}} with
\code{slot = "counts"} and no normalisation.
}
\seealso{
\code{simulate_bootstrap_samples()}, \code{simulate_generative_models()},
\code{vignettes/pseudo_bulk_generation.qmd}
}
