% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_pseudo_bulk_samples.R
\name{aggregate_celltype_barcode_pseudo_bulk}
\alias{aggregate_celltype_barcode_pseudo_bulk}
\title{Aggregate pseudo-bulk samples by cell type and barcode}
\usage{
aggregate_celltype_barcode_pseudo_bulk(
  seurat_obj,
  celltype_col = "luque_cluster_annotation",
  phenotype_col = "Morphotype",
  barcode_col = "Sample.barcode",
  assay = "RNA",
  cell_mask = NULL
)
}
\arguments{
\item{seurat_obj}{A \pkg{Seurat} object with raw counts in the RNA assay.}

\item{celltype_col}{Metadata column for cell-type labels
(e.g. \code{"luque_cluster_annotation"}).}

\item{phenotype_col}{Metadata column for organoid-level phenotype labels
(e.g. \code{"Morphotype"}).}

\item{barcode_col}{Metadata column for barcode / organoid identifiers
(e.g. \code{"Sample.barcode"}).}

\item{assay}{Assay name from which raw counts are read. Default
\code{"RNA"}.}

\item{cell_mask}{Optional logical vector aligned with \code{seurat_obj}
cells; when provided, only these cells are aggregated.}
}
\value{
A \pkg{SummarizedExperiment} with one column per distinct
cell type x barcode pair. \code{colData} contains \code{celltype},
\code{barcode_id}, the phenotype label, \code{n_cells},
\code{library_depth}, and
\code{simulation_method = "celltype_barcode_aggregation"}.
}
\description{
Extension of \code{aggregate_barcode_pseudo_bulk()}: sum raw single-cell counts
within each cell type x barcode combination, yielding one pseudo-bulk
column per organoid within each annotated cell type. Uses the same raw-count
aggregation principle as Strategy 1 in
\code{vignettes/pseudo_bulk_generation.qmd}.
}
\seealso{
\code{aggregate_barcode_pseudo_bulk()}, \code{simulate_bootstrap_samples()},
\code{simulate_generative_models()}, \code{vignettes/pseudo_bulk_generation.qmd}
}
