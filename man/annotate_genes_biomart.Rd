% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_genes.R
\name{annotate_genes_biomart}
\alias{annotate_genes_biomart}
\title{Annotate mouse genes with Ensembl metadata}
\usage{
annotate_genes_biomart(
  genes,
  drop_missing_genes = TRUE,
  verbose = TRUE,
  batch_size = 200L
)
}
\arguments{
\item{genes}{Character vector of mouse gene symbols
(for example, \code{c("Gnai3", "H19")}).}

\item{drop_missing_genes}{Logical; whether to drop genes
that are not found in the annotation database. Default is \code{TRUE}.}

\item{verbose}{Logical; whether to print progress messages.}

\item{batch_size}{Maximum number of gene symbols per BioMart request.
Large inputs are split automatically to reduce timeouts.}
}
\value{
A \code{data.frame} with one row per input gene and columns:
\code{gene_name}, \code{ensembl_id}, \code{biological_function}, \code{gene_length},
\code{chromosome_name}, \code{gene_biotype}, and \code{ambiguous_genes}.
}
\description{
Query Ensembl BioMart for mouse genes and return a collapsed, one-row-per-gene
summary including Ensembl IDs, description(s), estimated gene length, and
genomic coordinates. If multiple Ensembl records map to one gene symbol, the
function concatenates text fields and averages gene lengths.
}
\details{
Output structure matches \code{\link[=annotate_genes_local]{annotate_genes_local()}}.
}
\examples{
\dontrun{
annotate_genes_biomart(c("Gnai3", "H19", "Scml2"))
}
}
