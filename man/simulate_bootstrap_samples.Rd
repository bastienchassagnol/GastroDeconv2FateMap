% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_pseudo_bulk_samples.R
\name{simulate_bootstrap_samples}
\alias{simulate_bootstrap_samples}
\title{Simulate pseudo-bulk samples by cell resampling (bootstrap)}
\usage{
simulate_bootstrap_samples(
  seurat_obj,
  phenotype_col = "Morphotype",
  barcode_col = "Sample.barcode",
  n_samples = 100L,
  replicate_type = c("biological", "technical"),
  cells_per_sample = NULL,
  assay = "RNA",
  seed = NULL
)
}
\arguments{
\item{seurat_obj}{A \pkg{Seurat} object with raw counts.}

\item{phenotype_col}{Metadata column for phenotype labels.}

\item{barcode_col}{Metadata column for barcode identifiers.}

\item{n_samples}{Total number of pseudo-bulk samples to generate.
Phenotypes are allocated as evenly as possible across samples.}

\item{replicate_type}{\code{"biological"} (phenotype-level pooling) or
\code{"technical"} (within-barcode resampling).}

\item{cells_per_sample}{Number of cells aggregated per pseudo-bulk sample.
Defaults to the median barcode cell count in the object.}

\item{assay}{Assay from which raw counts are read.}

\item{seed}{Optional random seed for reproducibility.}
}
\value{
A \pkg{SummarizedExperiment} with raw summed counts and balanced
phenotypes in \code{colData}.
}
\description{
Strategies 2 and 3 from \code{vignettes/pseudo_bulk_generation.qmd}.
\describe{
\item{\code{replicate_type = "biological"}}{
Phenotype-stratified bootstrap: resample cells from all organoids
sharing the target phenotype (strategy 2).}
\item{\code{replicate_type = "technical"}}{
Organoid-preserving bootstrap: resample cells within a single barcode
before aggregation (strategy 3).}
}
}
\seealso{
\code{aggregate_barcode_pseudo_bulk()}, \code{simulate_generative_models()}
}
