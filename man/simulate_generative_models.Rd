% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_pseudo_bulk_samples.R
\name{simulate_generative_models}
\alias{simulate_generative_models}
\title{Simulate pseudo-bulk samples from generative count models}
\usage{
simulate_generative_models(
  seurat_obj,
  phenotype_col = "Morphotype",
  barcode_col = "Sample.barcode",
  n_samples = 100L,
  model = c("lognormal", "negative_binomial"),
  cells_per_sample = NULL,
  assay = "RNA",
  seed = NULL
)
}
\arguments{
\item{seurat_obj}{A \pkg{Seurat} object with raw counts.}

\item{phenotype_col}{Metadata column for phenotype labels.}

\item{barcode_col}{Metadata column for barcode identifiers.}

\item{n_samples}{Total pseudo-bulk samples (balanced across phenotypes).}

\item{model}{\code{"lognormal"} for log-normal gene counts, or
\code{"negative_binomial"} for Splatter-style negative-binomial counts.}

\item{cells_per_sample}{Cells simulated and summed per pseudo-bulk sample.}

\item{assay}{Assay from which raw counts are read.}

\item{seed}{Optional random seed.}
}
\value{
A \pkg{SummarizedExperiment} with raw summed counts.
}
\description{
Splatter- and HADACA3-inspired generative simulation (strategies 7 and 10
in \code{vignettes/pseudo_bulk_generation.qmd}). Parameters are inferred at
the biological (phenotype) level; technical library scaling is applied
only for the log-normal model. Simulated cells are summed to sample-level
raw bulk counts.
}
\details{
\strong{Log-normal path}: per-gene \eqn{(\hat{\mu}_g, \hat{\sigma}_g)} on
\eqn{\log(1+x)} scale per phenotype, with barcode library scaling
\eqn{s_b} at simulation time.

\strong{Negative-binomial path}: per-gene \eqn{(\hat{\mu}_g, \hat{\theta}_g)}
via method-of-moments (HADACA3-style) and per-cell library factors from
\code{splatter::splatEstimate}. No extra barcode scaling is applied,
because Splatter already models library-size variation.

Bulk aggregation: \deqn{Y_{gi} = \sum_{c \in \mathcal{C}_i} x_{gc}^{\mathrm{sim}}}
}
\seealso{
\code{aggregate_barcode_pseudo_bulk()}, \code{simulate_bootstrap_samples()},
\url{https://oshlacklab.com/splatter/articles/splatter.html}
}
