% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{summarise_seurat_assays_layers}
\alias{summarise_seurat_assays_layers}
\title{Print a hierarchical summary of assays and layers}
\usage{
summarise_seurat_assays_layers(seurat_obj)
}
\arguments{
\item{seurat_obj}{A \pkg{Seurat} object (v4/v5), with one or more assays.}
}
\value{
\code{NULL}, invisibly. The summary is printed to the standard
output connection via \code{\link[base]{cat}}.
}
\description{
\strong{Why RNA can be ~27k genes while \code{dim(seurat)} is ~2944 features}:
\itemize{
\item \code{dim(seurat)} uses \code{DefaultAssay()} (here often \code{"integrated"}).
\item The integrated / SCT assays keep the integration anchor feature set.
\item Assay \code{"RNA"} still holds the full transcriptome (all detected
genes).
}
\strong{LayerData} (see \code{?SeuratObject::LayerData}): main arguments:
\itemize{
\item \code{layer}: e.g. \code{"counts"}, \code{"data"}, \code{"scale.data"}, or \code{NULL} for
the default layer(s).
\item \code{features}, \code{cells}: optional row/column subsets (Assay / Assay5).
\item \code{assay}: when \code{object} is a Seurat, which assay to query.
\item \code{search} in \code{SeuratObject::Layers}: \code{NA} = all layer
names; \code{NULL} = default layer(s).
}
\strong{Default layer}:
\itemize{
\item Read: \code{SeuratObject::DefaultLayer} on an assay.
\item Set: \code{DefaultLayer(assay) <- "counts"} (or another existing layer).
\item Reading defaults: \code{LayerData(seurat, assay = "RNA", layer = NULL)}
uses the default layer for that assay (often \code{"data"}).
}
}
\seealso{
\link[SeuratObject]{LayerData}, \link[SeuratObject]{Layers},
\link[SeuratObject]{DefaultLayer}
}
\keyword{internal}
