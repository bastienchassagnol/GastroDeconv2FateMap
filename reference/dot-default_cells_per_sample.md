# Default number of cells per pseudo-bulk sample

Returns the median barcode cell count in `meta`.

## Usage

``` r
.default_cells_per_sample(meta, barcode_col)
```

## Arguments

- meta:

  Cell metadata `data.frame`.

- barcode_col:

  Column name for barcode identifiers.

## Value

Integer scalar.
