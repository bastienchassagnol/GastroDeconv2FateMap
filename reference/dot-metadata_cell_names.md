# Resolve metadata rows to Seurat cell names

Resolve metadata rows to Seurat cell names

## Usage

``` r
.metadata_cell_names(meta, seurat_obj, assay)
```

## Arguments

- meta:

  Seurat metadata.

- seurat_obj:

  A Seurat object.

## Value

Character vector aligned with `meta`.
